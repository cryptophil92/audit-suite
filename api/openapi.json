{
  "openapi": "3.0.3",
  "info": {
    "title": "AUDIT-SUITE Local Read-only API",
    "version": "0.2.34",
    "description": "Local read-only API for AUDIT-SUITE JSON endpoints."
  },
  "servers": [
    {
      "url": "http://127.0.0.1:8765"
    }
  ],
  "paths": {
    "/api/health": {
      "get": {
        "summary": "Health check",
        "responses": {
          "200": {
            "description": "API health payload"
          }
        }
      }
    },
    "/api/status": {
      "get": {
        "summary": "Local engine status",
        "responses": {
          "200": {
            "description": "Status JSON payload"
          },
          "502": {
            "description": "Subprocess output limit exceeded"
          },
          "504": {
            "description": "Subprocess timeout"
          }
        }
      }
    },
    "/api/modules": {
      "get": {
        "summary": "Available module catalog",
        "responses": {
          "200": {
            "description": "Modules JSON payload"
          },
          "502": {
            "description": "Subprocess output limit exceeded"
          },
          "504": {
            "description": "Subprocess timeout"
          }
        }
      }
    },
    "/api/history": {
      "get": {
        "summary": "Run history list",
        "responses": {
          "200": {
            "description": "History JSON payload"
          },
          "502": {
            "description": "Subprocess output limit exceeded"
          },
          "504": {
            "description": "Subprocess timeout"
          }
        }
      }
    },
    "/api/history/paths": {
      "get": {
        "summary": "History storage paths",
        "responses": {
          "200": {
            "description": "History paths JSON payload"
          },
          "502": {
            "description": "Subprocess output limit exceeded"
          },
          "504": {
            "description": "Subprocess timeout"
          }
        }
      }
    },
    "/api/latest": {
      "get": {
        "summary": "Latest run",
        "responses": {
          "200": {
            "description": "Latest run JSON payload"
          },
          "502": {
            "description": "Subprocess output limit exceeded"
          },
          "504": {
            "description": "Subprocess timeout"
          }
        }
      }
    },
    "/api/run": {
      "get": {
        "summary": "Verified run detail",
        "parameters": [
          {
            "name": "run_id",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Run detail with report inventory and export review"
          },
          "400": {
            "description": "Invalid or missing run identifier"
          },
          "502": {
            "description": "Subprocess output limit exceeded"
          },
          "504": {
            "description": "Subprocess timeout"
          }
        }
      }
    },
    "/api/report": {
      "get": {
        "summary": "Verified local HTML report",
        "parameters": [
          {
            "name": "run_id",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"
            }
          },
          {
            "name": "kind",
            "in": "query",
            "required": true,
            "schema": {
              "type": "string",
              "enum": ["private", "shareable", "technical"]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Verified report rendered with an isolated CSP"
          },
          "400": {
            "description": "Invalid or missing query parameter"
          },
          "404": {
            "description": "Run or report not available"
          },
          "502": {
            "description": "Subprocess output limit exceeded"
          },
          "504": {
            "description": "Subprocess timeout"
          }
        }
      }
    },
    "/api/snapshot": {
      "get": {
        "summary": "Aggregated API snapshot",
        "responses": {
          "200": {
            "description": "Snapshot JSON payload"
          },
          "502": {
            "description": "Subprocess output limit exceeded"
          },
          "504": {
            "description": "Subprocess timeout"
          }
        }
      }
    },
    "/api/plan": {
      "get": {
        "summary": "Plan preview",
        "parameters": [
          {
            "name": "targets",
            "in": "query",
            "required": true,
            "schema": { "type": "string" }
          },
          {
            "name": "profile",
            "in": "query",
            "required": false,
            "schema": { "type": "string", "enum": ["fast", "full", "stealth"], "default": "fast" }
          },
          {
            "name": "categories",
            "in": "query",
            "required": false,
            "schema": { "type": "string", "default": "all" }
          },
          {
            "name": "run_id",
            "in": "query",
            "required": false,
            "schema": { "type": "string" }
          },
          {
            "name": "no_zeek",
            "in": "query",
            "required": false,
            "schema": { "type": "boolean" }
          },
          {
            "name": "no_suricata",
            "in": "query",
            "required": false,
            "schema": { "type": "boolean" }
          }
        ],
        "responses": {
          "200": {
            "description": "Plan JSON payload"
          },
          "400": {
            "description": "Invalid or missing query parameter"
          },
          "502": {
            "description": "Subprocess output limit exceeded"
          },
          "504": {
            "description": "Subprocess timeout"
          }
        }
      }
    },
    "/api/openapi.json": {
      "get": {
        "summary": "OpenAPI specification",
        "responses": {
          "200": {
            "description": "OpenAPI JSON document"
          }
        }
      }
    }
  }
}
